import { searchDiscoverStories } from "@/data/discover";

export { searchDiscoverStories };

/** Homepage and header search. Off on the public site until the query bugs are fixed. */
export const SEARCH_UI_ENABLED = false;
